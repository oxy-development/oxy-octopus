/**
 * Created by ygintsyak on 22.08.16.
 */

'use strict';

const initializer = require('../rpc-method-initializer');

/**
 * Helps to observe project's source tree
 * @type {{name, factory}|{name: *, factory: *}}
 */
module.exports = initializer('project.ls', (project) => (params, callback) => {

    //TODO. Implement me
});